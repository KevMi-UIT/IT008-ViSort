<Page x:Class="ViSort.Pages.SortsInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ViSort.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:views="clr-namespace:ViSort.Views"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1300"
      ui:Design.Background="White"
      Title="SortsInfoPage">

    <Grid Margin="20,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TextBlock Text="Sorts Info" FontTypography="TitleLarge" Grid.Row="0" />
        <Separator Margin="30,10" Grid.Row="1"/>
        <Grid Grid.Row="2" VerticalAlignment="Top">
            <Grid Margin="80,0,80,50" MaxWidth="400">
                <ui:AutoSuggestBox
                    x:Name="AutoSuggestBox"
                    PlaceholderText="Sort..."
                    Icon="{ui:SymbolIcon Search24}"
                    QuerySubmitted="AutoSuggestBox_QuerySubmitted"/>
            </Grid>
            <views:SortsInfoView x:Name="SortsInfoViewSection" DataContext="{Binding }"/>
        </Grid>
    </Grid>
</Page>
